<%= form_for(m_country) do |f| %>
  <% if m_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_country.errors.count, "error") %> prohibited this m_country from being saved:</h2>

      <ul>
      <% m_country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ncountry %>
    <%= f.text_field :ncountry %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
