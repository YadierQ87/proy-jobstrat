<p id="notice"><%= notice %></p>
<%= render "bloques/admin_section" %>
<br>
<div class="panel">
  <div class="panel panel-default">
    <div class="panel-heading">
      Admin Aplication-Jobs
    </div>
    <div class="panel-body">
      <div class="row">
        <div class="col-lg-6">
          <%= form_for_filterrific @filterrific do |f| %>
              Records to show
              <%= select_tag :cantd, options_for_select([5,10,20,40].to_a, nil)  %>
              Search
              <%# give the search field the 'filterrific-periodically-observed' class for live updates %>
              <%= f.text_field(   :search_query, class: 'filterrific-periodically-observed' ) %>
              <%# add an automated spinner to your form when the list is refreshed %>
              <%= render_filterrific_spinner %>
          <% end %>
          <%#= link_to( 'Reset filters', reset_filterrific_url, class: "btn-normal btn-sm") %>
        </div>
        <div class="col-lg-6">
          <div class="right">
            Download:
            <%= link_to icon('file-code-o','CSV'), aplicationjob_path(format: "csv") %> |
            <%= link_to icon('file-excel-o','Excel'), aplicationjob_path(format: "xls") %>
            <%= link_to icon('plus-circle','Add Aplication-Job'), new_aplicationjob_path , class: "btn-primary btn-sm" %>
          </div>
        </div>
      </div>
      <hr>
      <%= render(        partial: 'aplicationjobs/list',        locals: { aplicationjobs: @aplicationjobs }    ) %>
    </div>
    <div class="panel-footer">
      &nbsp;<i class="right">aplication jobs</i>
    </div>
  </div>
</div>


